<table class="table table-hover course clickable">
  <thead>
    <tr>
      <th>#</th>       
      <th>Name</th>       
      <th>Note</th>       
    </tr>
  </thead>

  <tbody>
    <% @courses.each do |c| %>
      <tr data-link="/courses/<%= c.id %>" data-course-id="<%= c.id %>">
        <th><%= c.id %></th>       
        <th><%= c.name %></th>       
        <th><%= c.note %></th>       
      </tr>
    <% end %>
  </tbody>
</table>

